能。建議做法如下：

1) 使用 OpenAI / ChatGPT 的影像生成 API（或其他圖像 API）：
   - 將 5 個提示詞放入陣列，寫一個程式（例如 Python），逐一呼叫 API、等待回應並儲存圖檔，再處理下一個提示詞。
   - 優點：可靠、可控、合乎使用條款；可加入重試、速率限制與檔名管理。

2) 替代方案：
   - 本地模型（如 Stable Diffusion）：完全離線，可同樣用腳本序列化產生。
   - 瀏覽器自動化（Selenium/Playwright）：技術可行但可能違反服務條款，不建議。

3) 注意事項：
   - 需 API 金額與速率配額考量；需處理失敗重試與儲存路徑。

是否要範例程式（Python 範例，含等待與存檔）？

檔案已寫入：artifacts/4517504963/result.md
